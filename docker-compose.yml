services:
  chegini-proposal:
    build: .
    ports:
      - "3007:3007"
      - "3008:3008"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true

